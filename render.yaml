services:
  - type: web
    name: scout-email-finder
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 3001
      - key: GOOGLE_MAPS_API_KEY
        sync: false
    healthCheckPath: /
